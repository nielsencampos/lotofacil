lotofacil:
  target: dev
  outputs:
    dev:
      type: postgres
      host: "{{ env_var('POSTGRES_HOST', 'localhost') }}"
      port: "{{ env_var('POSTGRES_PORT', '5432') | as_number }}"
      dbname: "{{ env_var('POSTGRES_DB', 'lotofacil') }}"
      user: "{{ env_var('POSTGRES_USER', 'lotofacil') }}"
      password: "{{ env_var('POSTGRES_PASSWORD', 'lotofacil') }}"
      schema: public
      threads: 4
